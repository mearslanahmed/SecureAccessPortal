using System;

namespace SecureAccessPortal.Controls
{
    // Code-behind for the Top Navigation User Control
    public partial class TopNav : System.Web.UI.UserControl
    {
        protected void Page_Load(object sender, EventArgs e)
        {
            // Currently, no server-side logic is needed.
            // This control is primarily used for consistent navigation UI across pages.
        }
    }
}
